import React from 'react';
import { ICardField } from '../controllers/CardField';

export interface ICardFieldProps {
  field: ICardField;
  canEditKey: boolean;
  editing: boolean;
}

export const CardField: React.VoidFunctionComponent = () => <></>;
